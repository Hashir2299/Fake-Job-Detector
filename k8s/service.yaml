apiVersion: v1
kind: Service
metadata:
  name: fake-job-detector-service
spec:
  type: NodePort
  selector:
    app: fake-job-detector
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
